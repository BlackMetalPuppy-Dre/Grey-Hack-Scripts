Start mapkit [IP target] BROKEN!!!

Full port scanning – finds open ports and services

Router scan – scans kernel_router.so for exploits

Service scan – scans SSH, HTTP, FTP, SQL, SMTP

Exploit database – saves all results to /targets/

Attack phase – pick a library → pick an exploit → fire it

SSH direct login – manual SSH access

Port override – change port when firing exploits

Passwd decryption – decrypts /etc/passwd files

Made with Claude AI and Deepseek (free versions)
