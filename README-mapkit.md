Start mapkit [IP target]

Full port scanning – finds open ports and services

Router scan – scans kernel_router.so for exploits

Service scan – scans SSH, HTTP, FTP, SQL, SMTP
When shell given it asks for user input for what kind of privileges you get 1 root 2 guest 3 user 4 custom name

Exploit database – saves all results to /targets/

Attack phase – pick a library (gives version of the libservices that are running on IP removes closed ports) → pick an exploit → fire it

SSH direct login – manual SSH access

Passwd decryption – decrypts /etc/passwd files (does not work properly yet) 

Made with Claude AI and Deepseek (free versions)
